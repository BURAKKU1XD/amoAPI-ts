/**
 * Interface for entities that can return deleted items
 */
export interface CanReturnDeletedInterface {
  // Constant for filtering only deleted entities
}

export const ONLY_DELETED = 'only_deleted';
